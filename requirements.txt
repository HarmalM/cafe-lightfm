# CAFE-LightFM — Colab-compatible requirements
# Python 3.12 + CUDA 12.8 (Colab default as of 2026)

# ── Pre-installed in Colab (verify only, do not reinstall) ────────────────
# torch==2.11.0+cu128
# numpy==2.0.2
# scipy==1.16.3
# scikit-learn==1.6.1
# pandas==2.2.2
# matplotlib
# joblib==1.4.2

# ── Install in Colab setup cell ───────────────────────────────────────────
pyyaml>=6.0.1

# ── LightFM baseline note ─────────────────────────────────────────────────
# lightfm==1.17 is incompatible with Python 3.12 (ob_digit removal, PEP 659)
# Baseline MF is implemented natively in PyTorch — see models/baselines/mf_baseline.py
# lightfm installation will be attempted in experiments/baselines/ only
